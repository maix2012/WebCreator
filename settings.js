module.exports = {
  // 服务器启动提示
  // 可用变量占位符:
  // ${port} - 服务器端口号
  // ${env} - 当前环境(development/production)
  // ${time} - 服务器启动时间
  startupMessage: "欢迎使用精灵建站工具包，服务器已启动，访问 http://localhost:${port}"
};